# Bounded next steps — not launched or funded beyond the existing allocation

1. Run only the prepared Wet-June world-0 `progression_v8.yaml` development
   assignment first. Retain every rejected proposal, native rejection and
   termination. Assess full planting coverage, all four policy decisions and
   harvest/storage. If it fails, inspect that trace before paying for more
   scenarios or held-out runs. The memory change is not yet behaviorally proven.
2. If it passes, run Disease–Drought and Three-cultivar development progression
   with the same controller repair and explicit manifests, followed by bounded
   compatibility checks where necessary. Do not infer completion from adapter
   compatibility or a saved `COMPLETED` marker.
3. Drought needs a timing-controlled native diagnostic alongside the retained
   total-effect omission: the current omission removes both water input and
   native operation duration. Define the estimand and sham timing prospectively
   on development worlds before implementation/measurement. Do not change a
   failed marketable-yield check to biological yield, discard negative outcomes,
   or attribute reversals to clock timing without a controlled comparison.
4. After a defensible candidate and development progression pass, freeze exact
   settings and unused cohorts. Worlds 20–24, 30–33 and 40–44 are consumed;
   reserved 34–35 are unlaunched. Any source revision after native confirmation
   requires a new applicable source binding and unused confirmation cohort.
5. Run matched communication smoke only after all three no-fault gates pass.
   Rebuild the review package, obtain genuine professor sign-off, and then
   enable the 1,245-run study and independent annotation.

## Budget decision needed

Current development $39.539415/$40; confirmation $40.147332/$60; total
$79.686747/$100. No new requests were made on 14 September for these repairs.
The $0.460585 development remainder is inadequate for a full-season check.

Proposed authorization, **not applied**: $200 cumulative cap, split $70
development / $130 confirmation, retaining the same ledger and all historical
charges. This allows staged live validation at previously observed costs,
but is not a guarantee that every gate will pass within the allocation.
Run one development season first; stop on failure or before the ledger cap.
Do not spend the professor's full-study budget during engineering work.
