# Professor handover status

**Not handover-ready. Not submission-ready.** The code and authored specifications
are implemented, but real-agent season progression has not passed. The professor
should not receive unresolved engineering work disguised as a released study.
Full experiments, professor approval and independent annotation have not occurred.

## Current evidence (13 September 2026)

| Gate | Saved evidence and current boundary |
| --- | --- |
| PDF closure | `PDF_CLOSURE_CHECKLIST.md` maps recommendations to implementation, tests and remaining empirical tasks. Pending tasks are not passed gates. |
| Offline correctness | **371/371 tests passed**, source digest unchanged; `handover_validation/confirmation_freeze_regression.json`. Earlier 364/356/353/345/309 checkpoints remain preserved. |
| Authored specifications | 13 primary, sensitivity, team and refinement artifacts are generated by `build_authored_specs.py`. Optional refined-team handoffs now have acceptance predicates. Author-defined does not mean independently confirmed or professor-approved. |
| Native workflows | All three authored reference workflows complete harvest/storage; event fidelity is 1.0. Wet-June, drought and Three-cultivar causal conformance are 0.421875, 0.25 and 0.388889. These are specification-relative development diagnostics. See `results/aamas_handover/authored_native_v6/summary.json`. |
| Drought development | The separately declared `drought_pulse_v4` / `authored_harvest_calendar_v5` candidate passes all ten reserved development worlds 0–9: accepted irrigation, 100% target-root stress, complete pairs and omission losses 1.0921–2.6890%. Frozen confirmation now failed the all-pairs gate: 4/5 passed, world 20 had 0.30285% loss. All pairs completed. |
| Live progression | All three v6 world-0 pilots failed harvest/storage. Wet-June used 353 provider requests, drought 363, Three-cultivar 409. Observer completion, historical-failure confusion, repeated operations and scheduling waits were observed. Raw traces and outcomes are preserved; incomplete yields are unavailable as final outcomes. |
| Controller compatibility | All nine families and corrected 3-/4-agent adapters passed bounded checks on the actual study JSON provider, with no recorded controller/infrastructure failures. These are software checks, not completed seasons. |
| Direct/A2A baseline | Public briefing, authoritative export, actual delegation, structured final answers and study generation settings repaired. `baseline_compatibility_v5` uses the actual study JSON provider; direct made 12 requests, A2A made 9 with two completed delegations. Invalid proposals remain recorded; no unhandled specialist crash remains in this check. |
| Communication treatments | Deterministic phase/route/send-order tests exist. Final matched live audit/enforcement/free-text conditions await successful no-fault progression. Inactive assignments remain in intention-to-treat reporting. |
| Diagnostics | Controlled scope, freshness, provenance, execution failure, retry, refusal and uncertainty fixtures exist. Partial seasons retain unknown branch diagnoses and common obligations. Missing historical acceptance predicates stay unassessable. Independent natural-episode annotation remains pending. |
| Reproducibility | Fresh checkout `8720c73` installed offline, passed 8 new compatibility tests, regenerated all authored specifications and 29 table/support files identically, and passed doctor (`healthy=true`, `paper_ready=false`). The six-page manuscript compiles. Final subsequent fixes and archives still need verification. |
| Review / release | Both are blocked. Review requires passed engineering/smoke gates; release additionally requires genuine approval bound to identical reviewed content and satisfied scientific gates. |

The latest compact live evidence is `handover_validation/live_development_v6.json`.
Exact run identities, configurations, native receipts, rejected proposals and
provider usage remain under `results/aamas_handover/`. These artifacts are
engineering evidence and must not be promoted into paper results.

## Repairs prompted by the live checks

- Per-role persistent ledger allocations now agree with runtime allocations;
  unused observer tokens are not mistaken for an equal operations cap.
- Partial-season evaluation reports missing phase truth as unknown. Unexpected
  evaluation failures save native/distributed traces, process and outcome before
  raising; the refined-team failures demonstrated this capture path.
- Optional team handoffs receive acceptance predicates; historical gaps remain
  explicitly unassessable and ineligible for paper claims.
- Direct/A2A exports use the authoritative native trace schema. Provider accounting
  includes nested calls; actual completed expert calls establish delegation.
- Public-task overrides are installed before native briefing events are built.
  Setting them after initialization had left the old procedural briefing in place.
- Agent snapshots state the season-long assignment and distinguish scheduling waits
  from farm-time advancement. Historical failures carry result times, and an
  accepted retry marks recovery only for the same action and exact arguments.
  Finished agents are not revived and actions are not silently substituted.

## Scientific freeze and budget

The exact scenario variant now reaches runtime, compiler, preflight and both
native baselines. Confirmation validation binds scenario, calendar reference,
process, protocol, execution source and disjoint cohorts. The selected drought
candidate uses the already authored harvest window ending November 2; it does
not change native acceptance, rewards, the 50% stress criterion or 1% loss
criterion. Earlier relative-cap failures remain failed. See
`handover_development/DROUGHT_CALENDAR_REFERENCE_V5.md` and the complete ten-world
report at `results/aamas_handover/drought_calendar_v5_selected_dev0to9/`.

Confirmation worlds **20–24** have been consumed and failed the all-pairs criterion.
Live worlds **30–31** have six preserved pre-provider configuration failures.
The repaired live check prospectively uses new worlds **32–33**.
Study worlds remain **100–109**, secondary blocks **100–104**. The four manifests
retain **480 + 450 + 270 + 45 = 1245** assignments. No scenario was dropped.

The user explicitly authorized sending simulated context to OpenAI. The previous
API payload approval blocker is resolved. All calls share
`results/aamas_handover/spending.sqlite`: **$40 development / $60 confirmation,
$100 total**. Current live attempts may change the balance; the ledger is the
source of truth. No confirmation allocation has been spent.

## Preserved limitations and remaining work

Earlier paid attempts v1–v4 used 890 requests ($11.235421) and none completed a
season. v5 failed before provider invocation due to a relative-path defect. v5b
made 232 calls, hit the erroneous role allocation and then failed evaluation.
Its full trace was not saved by the old code: only failure/run identity and
provider ledger evidence survive. Do not imply a reconstructable native trace.
All subsequent v6 outcomes and the refined-team evaluation-failure captures exist.

Wet-June native window/recovery and Three-cultivar intervention evidence remains
exploratory (`handover_validation/native_interventions.csv` and PDF checklist).
Three-cultivar irrigation omission was weak; swapped-region treatment also changes
area. Reviewer numerical findings remain reviewer-provided until reproduced.

Next: complete the repaired live checks on prospectively declared worlds 32–33
and verify the final package. The failed drought gate remains a release blocker. Only
after those gates pass should the professor review package, portable raw archive,
hashes and tested tag be certified. Rebuild manuscript tables/PDF and verify the
fresh checkout and archives at that final revision.

The substantive non-results manuscript exists in official AAMAS format. Empirical
panels and resulting conclusions remain pending. The professor's full study and
60 double-annotated episodes are prerequisites for submission claims; acceptance
cannot be guaranteed by engineering readiness.

## Prospective final confirmation freeze

`confirmation_v1/FREEZE.json` binds all authored artifacts and sensitivity
alternatives, the protocol, execution source and explicit calibration/live
manifests. Calibration worlds 20–24 and live worlds 30–31 are still untouched
at freeze. Study settings remain 700 calls, 24M team tokens and 4096 output
tokens, with the pinned model and OpenAI JSON provider. Matched drop smoke
(three representations/enforcement conditions, both live worlds, 200 calls)
is declared before outcomes and may launch only after no-fault progression passes.

Development v7 reached August 12, planting and fungicide decisions, but both
actors exhausted their smaller 8M combined allocation before harvest/storage.
It made 441 requests costing $6.123678 under the historical uncached accounting.
This remains an incomplete outcome, not a successful season.

Development accounting now totals **$39.539415**. The final 12-call cache check
reported 80,896 cached input tokens and cost $0.073964. All 3,061 earlier ledger
rows are unchanged; a complete pre-migration snapshot is retained. New
settlement uses valid provider-reported cache counts, with full uncached
reservations and conservative fallback when usage is unknown. The $60
confirmation allocation is untouched at freeze. No threshold, reward, scenario
or completion criterion was weakened.

## Held-out results and runtime repair

The first confirmation is complete and failed. Drought worlds 20–24 have
omission losses **0.30285%, 1.55031%, 1.24662%, 1.30596%, 1.17389%**. Every
pair completed, irrigation was accepted and all target roots were stressed.
The first world fails the unchanged 1% requirement; no replacement calibration
cohort is launched to search for success. Exact paired telemetry and diagnostic
regeneration are in `handover_validation/drought_confirmation_diagnosis.json`.

All six live assignments on 30–31 failed before API invocation because the
resolved-team check retained the old 8M ceiling. The repair uses the already
validated team allocation and tests actual 2/3/4-agent resolution, including
overallocation rejection. `confirmation_v2/FREEZE.json` prospectively declares
32–33 for the repaired live checks; the old assignments remain failures.
No API cost was incurred by those six failures. A successful new live check
cannot override the failed drought scientific gate.
